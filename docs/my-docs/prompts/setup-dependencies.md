setup following for the project:
Use pyproject.toml as the main project/package definition.
Use a lockfile for reproducible installs.
Use a local virtual environment like .venv.
Separate runtime, dev, test, notebook/EDA dependencies.
Document setup commands in README.md.
