Goal is to create prepocessing steps based on 

Since this project include two use-case sale forcasting and segmentation. Pre-processing that are generic to both use-case, add at `src/demand_forecast_intelligence/data`, while specalised pre-processing and features
as mentioned, within src create core/, data/ and within domain add pre